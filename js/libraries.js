// Default Placeholder for all Library files to be included via CodeKit
/*
$(function(){
		$pos = $('#topnav').offset().top - 0;

		$(window).on('scroll', function(){
			if($(window).scrollTop() >= $pos) {
				$('#topnav').addClass('solid');
			} else {
				$('#topnav').removeClass('solid');
			}
		});
	});
*/;